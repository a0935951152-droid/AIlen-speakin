# 規則.md — 開發細則

天條在 CLAUDE.md;架構、事件 schema、Phase 進度的唯一真相來源是《結構及技術棧.md》。
本檔放日常開發需要的指令、架構導覽與已踩過的坑。

## 常用指令

venv 在本機 NVMe（`/home/a0935951152/speakin-venv`），repo 內 `.venv` 是指向它的 symlink
（venv 無 pip，裝套件用 `uv pip install --python .venv/bin/python`）：

```bash
.venv/bin/ruff check .                 # lint（CI 同款）
.venv/bin/python -m pytest tests/unit  # 單元測試（無 GPU/外部依賴，CI 會跑）
.venv/bin/python -m pytest tests/unit/test_events.py -k roundtrip  # 跑單一測試

# 重播測試需要 GPU + NATS + vLLM 都在線（CI 不跑）
.venv/bin/python -m pytest tests/replay
.venv/bin/python -m pytest tests/replay/test_livekit_tts.py  # 只需 GPU+NATS+LiveKit,免 vLLM

# 啟動依賴
docker start speakin-nats              # NATS JetStream（含 -m 8222 監控埠）
bash deploy/dev/vllm_mt.sh             # vLLM 起 breeze-7b-mt 於 :8001（見下方環境細則）
curl -s localhost:8001/v1/models       # 確認 vLLM 就緒
bash deploy/dev/livekit.sh             # LiveKit SFU（docker host network，dev 金鑰僅內網）

# 跑管線（dev 重播模式）：--set 可覆寫 pipeline YAML 任一節點參數（值過 YAML 解析）
.venv/bin/python -m services.runner --pipeline pipelines/default.yaml --session dev \
  --set stt.source=tests/replay/golden/zh_demo.wav --set stt.language=zh
.venv/bin/python -m services.viewer --session dev   # 終端訂閱者，看字幕流
```

## 架構（讀多個檔案才看得出來的部分）

核心原理：**stage 之間只透過 NATS topic 溝通、只依賴事件 schema**，因此管線拓撲就是
一份 YAML（`pipelines/*.yaml`），插入/替換 stage 不動核心碼。

- `schemas/events.py` — 事件 schema v1.0（`SegmentEvent` 等，`extra="forbid"`，擴充欄位走
  `meta`）。topic 命名一律用此處的 helper（`stt_topic`/`text_topic`/`tts_topic`），不要手拼字串。
- `core/bus/` — NATS 封裝；`nats-py` 只准出現在這個模組。
- `core/stage/` — `Stage` ABC（`setup`/`run`/`teardown`）與 `load_stage("name@ver")`，
  從 `stages/` 與 `plugins/` 找目錄（`manifest.yaml` + `stage.py` 的 `STAGE_CLASS`）。
- `services/runner/` — 讀 pipeline YAML、注入 config、併發跑各節點；`route_by` 節點會
  展開成多個 stage 實例（langs 注入 config，`*` 兜底拿 langs_exclude）。
- `stages/tts_cosyvoice2/` — CosyVoice2 程式碼 pip 裝不了，clone 在
  `~/speakin-data/CosyVoice`（stage 以 sys.path 引入）；venv 的 transformers 必須
  釘 4.51.3（5.x 預設 bf16 載入會讓合成輸出整段壞掉）；zh 合成前必經 OpenCC t2s。

### 事件流不變量（天條 #4 的細則，破壞會讓下游全錯）

- 每段語音有遞增 `rev` 的 `partial` 事件，最後一個 `final` 收尾（發過 partial 就
  必有 final，文字可為空 = tombstone：mt 原樣轉發、TTS 跳過、字幕端用以清掉假設）；
  下游以 `(speaker_id, segment_id, lang)` upsert，舊 `rev` 與 final 後事件須拒收。
- `segment_id` 貫穿 stt→mt→tts，不可分叉成多個 id；每個 stage 在 `trace` 追加自己的耗時。
- TTS 只消費 `final`。翻譯每段每目標語言只做一次，與訂閱人數無關。
- mt_vllm 有輸出語言護欄（`lang_guard_ok`）：擋模型對破碎 partial 原文照抄；改 prompt
  或換模型後要重跑 `tests/unit/test_mt_guard.py` 與 zh→en 重播確認。
- nats-py 同一訂閱的 callback 序列執行：多講者併發化（per-speaker 佇列）排在 Phase 4，
  同講者必須保序。

## 環境細則

- vLLM 用主機既有 0.22.1（MISA 的），不裝進 venv；無 sudo 環境的 Python 標頭、
  `CPATH`、`VLLM_USE_FLASHINFER_SAMPLER=0` 等解法已固化在 `deploy/dev/vllm_mt.sh`，
  不要繞過該腳本手起。
- 模型 registry 在 `/nas-data/allen-speakin/models`（git 忽略）、黃金音檔入庫以小檔為限；
  模型熱快取在 `~/speakin-data/model-cache`。
- GitHub：`gh` 未登入，token 在 `~/.git-credentials`（API 查 CI log 可借用）。

## CI 陷阱（2026-06-12 修過，別改回去）

- runner 系統 Python 受 PEP 668 保護，`uv pip install --system` 會被拒。
- setup-uv 設 `python-version` 時已預建 `.venv`，要 `uv venv --clear`。
- `uv run pytest` 不把 CWD 加 `sys.path`，靠 pyproject 的
  `[tool.pytest.ini_options] pythonpath = ["."]`，勿刪。
- 新增頂層 import 的依賴時，同步 `ci.yml` 的安裝清單（如 numpy）。
